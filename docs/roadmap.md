# ROADMAP: Notion 포트폴리오 쇼케이스 (진행 중)

> **이 문서의 역할**: `docs/roadmap.md`는 **현재 진행 중이거나 예정된** 개발 로드맵을 관리하는 메인 문서(살아있는 문서)입니다.
> 하나의 버전이 완료되면 이 문서의 내용을 `docs/roadmaps/roadmap_vN.md`로 아카이브하고, 이 문서는 다음 버전(V+1)의 계획으로 새로 채웁니다.
>
> - MVP(V1)는 이미 완료되어 [`docs/roadmaps/ROADMAP_v1.md`](./roadmaps/ROADMAP_v1.md)에 아카이브되어 있습니다. (수정 금지 — 과거 기록 보존용)
> - 지금 이 문서는 **V2** 계획을 다룹니다. 이번 갱신(2026-07-29)은 5가지 요구사항(아래 "검토 항목 개요")을 반영해 V2 범위를 대폭 재검토/확장한 버전입니다.
> - **0단계·1단계는 완료되었습니다** (아래 완료 기준 전부 `[x]`). 이번 추가 갱신(2026-07-29, 2차)에서는 코드를 다시 직접 확인해 2~4단계 서술의 정확성을 검증하고, 2~4단계 및 아카이빙 절차를 실행 가능한 구체적 계획으로 채웠습니다. 2차 검증 결과는 각 단계의 "코드 기준 재확인" 절에 반영되어 있습니다.

> ⚠️ 참고: 이 프로젝트는 Next.js 16 기반이며 학습 데이터 상의 관례와 다른 breaking change가 있을 수 있습니다.
> `AGENTS.md`는 App Router 라우팅/데이터 페칭 코드 작성 전 `node_modules/next/dist/docs/`를 확인하라고 안내하지만, **이 경로는 실제로 존재하지 않음이 `shrimp-rules.md`에서 이미 확인됨**. 대신 `context7` MCP로 Next.js 16 공식 문서를 조회한다.
> 이번 갱신에서 `context7`(`/vercel/next.js/v16.2.9`)로 재확인한 중요 breaking change: **Next.js 16에서는 `middleware.ts`가 `proxy.ts`로 이름이 바뀌었다** (인증/접근 제어를 다루는 4단계에 직접 영향).

---

## 버전 관리 규칙

- 이 저장소의 로드맵은 `docs/roadmap.md`(현재 진행 중) + `docs/roadmaps/roadmap_vN.md`(완료된 과거 버전) 구조로 관리합니다.
- **아카이브 시점**: 한 버전(V2, V3, ...)의 모든 핵심 단계가 완료 기준을 충족하면, 그 시점의 `docs/roadmap.md` 내용을 `docs/roadmaps/roadmap_v{N}.md`로 복사/이동하고, `docs/roadmap.md`는 다음 버전 계획으로 새로 작성합니다.
- 파일명 규칙: 기존 MVP는 `ROADMAP_v1.md`(대문자)로 이미 존재하므로 그대로 유지하고, V2부터는 소문자 `roadmap_v2.md`, `roadmap_v3.md`, ... 형식을 사용합니다.
- `CLAUDE.md`의 "개발 로드맵" 참조는 항상 `docs/roadmap.md`(현재 버전)를 가리키도록 고정합니다. 과거 버전을 찾을 때는 `docs/roadmaps/` 디렉터리를 확인하면 됩니다.

---

## 현재 목표 (V2)

MVP(V1)에서 Out of Scope로 명시했던 기능 중 **프로젝트 상세 페이지**를 축으로, 사용자가 이번에 확정한 5가지 요구사항을 반영해 V2 범위를 재구성합니다. MVP에서 구축한 Notion 연동 레이어(`lib/notion.ts`), 타입(`types/project.ts`), 카드/필터 컴포넌트(`components/project-card.tsx`, `components/tag-filter.tsx`) 등 기존 자산을 최대한 재사용하는 것을 원칙으로 합니다.

### 검토 항목 개요 (사용자 확정 원문 기준)

1. **프로젝트 상세 페이지 뎁스 구조** — 문제 정의/목표/과정/결과로 구성된 케이스 스터디 페이지.
2. **관리자 Dashboard** — 프로젝트 상태/콘텐츠/업데이트 현황을 확인하는 관리자용 대시보드.
3. **프로젝트/콘텐츠 CMS 구조 개선** — Notion 기반 CMS를 확장 가능한 구조로 개선(케이스 스터디 콘텐츠 저장 방식 포함).
4. **프로젝트 필터링** — 프로젝트 증가 시 탐색성을 높이는 카테고리/태그 기반 필터링.
5. **포트폴리오 목적에 맞는 추가 고도화 기능 제안** — 사용자 원문: *"추가로 포트폴리오 웹사이트라는 목적을 고려해서 필요한 고도화 기능이 있다면 우선순위와 함께 제안해줘."* (최초 전달 시 문장이 잘려 "사용자 확인 필요"로 임시 표기했으나, 이후 코디네이터를 통해 원문 전체를 확인함 — 아래 4단계에서 정식 제안으로 다룸)

---

## 우선순위 (V2 전체)

| 우선순위 | 항목 | 근거 요약 |
|---|---|---|
| 0 (선행 결정, Must) | CMS 구조 확장 방향 결정 (요구사항 3) | 1단계(상세 페이지 콘텐츠 방식)의 전제 조건. 결정 없이는 1단계 착수 불가 |
| 1 (핵심, Must) | 프로젝트 상세 페이지 — 케이스 스터디 구조 (요구사항 1 + 5-A) | V2의 중심 기능. 0단계 결정에 의존 |
| 2 (Should, 0/1단계와 병행 가능) | 필터링 고도화 (요구사항 4 — **신규 기능 아님, 기존 `TagFilter`/`ProjectShowcase` 개선**) | 코드 확인 결과 이미 다중 선택 OR 필터링이 구현되어 있음. 남은 개선점만 다룸 |
| 3 (Should, 즉시 착수 가능한 Quick win 포함) | 포트폴리오 목적 고도화 제안 (요구사항 5) | 채용/클라이언트 설득력 향상 목적. 일부는 기존 미사용 자산(`ContactForm`) 활용만으로 해결 가능 |
| 4 (Could, 비판적 검토 결과 범위 축소 권장) | 관리자 Dashboard (요구사항 2) | PRD의 "Notion = CMS" 설계 전제와 충돌 가능성 있음. 쓰기 가능 CRUD는 비권장, 읽기 전용 요약만 Could로 남김 |
| 낮음 (V2 범위 밖, 향후 후보) | 검색, 페이지네이션/무한스크롤, 다국어, 방문자 분석 | V1 Out of Scope. 이번 5가지 요구사항 중 어디에도 명시적으로 포함되지 않아 Must로 올리지 않음 |

---

## 0단계: CMS 구조 확장 방향 결정 (요구사항 3)

### 배경
1단계(케이스 스터디: 문제 정의/목표/과정/결과)를 구현하려면 Notion 쪽에 "본문 콘텐츠를 어디에 담을지"부터 결정해야 합니다. 현재 `types/project.ts`의 `Project`에는 본문 필드가 전혀 없고, `lib/notion.ts`는 속성(property) 기반 파싱만 합니다. 이 결정은 1단계뿐 아니라(케이스 스터디 저장 방식), 필요 시 4단계 필터링의 "카테고리/태그 분리" 논의와도 연결됩니다.

### 옵션별 트레이드오프

| 옵션 | 저장 위치 | 난이도 | 장점 | 단점 |
|---|---|---|---|---|
| A. 속성(property) 확장 | Notion DB에 "문제정의/목표/과정/결과" 4개 Rich Text 속성 추가 | 하 | `lib/notion.ts`의 기존 파싱 패턴(`getPlainText` 등) 그대로 재사용, 구조가 명확히 고정됨 | Notion 속성 패널은 서식(이미지/리스트/헤딩)이 자유롭지 않아 "케이스 스터디"라는 서사적 콘텐츠엔 표현력이 부족함. 속성 개수가 늘어나면 Notion DB 테이블 뷰가 복잡해짐 |
| B. 페이지 본문 블록 | 프로젝트 페이지 자체 본문에 헤딩(H2: 문제 정의/목표/과정/결과)으로 구분해 자유 작성, `notion.blocks.children.list()`로 조회 | 상 | 이미지/리스트/임베드 등 자유로운 서식 가능. PRD의 CMS 선택 이유("비개발자도 Notion에서 관리")와 가장 자연스럽게 맞음 — 원래 Notion 페이지를 쓰는 방식 그대로 | 블록 타입별 React 렌더러(문단/헤딩/이미지/리스트 등 재귀 처리)를 새로 구현해야 함. "4개 섹션"이라는 구조가 헤딩 컨벤션에 의존하므로 강제되지 않음(작성자가 헤딩을 빠뜨리면 파싱 실패 위험) |
| C. 연결된 서브 데이터베이스(관계형 필드) | 케이스 스터디 섹션을 별도 하위 DB의 행으로 저장, 프로젝트 DB와 relation 속성으로 연결 | 상 (C 중에서도 가장 높음) | 섹션 단위로 구조화된 조회/재사용 가능 | `context7`로 Notion SDK 문서 확인 결과, relation 속성은 `notion.pages.properties.retrieve()`로 관련 페이지 **ID 배열만** 반환하며 각 페이지의 실제 내용은 페이지별로 추가 조회해야 함(N+1 호출 패턴). 프로젝트 1건당 섹션 4개 고정이라는 단순한 구조에는 관계형 서브 DB가 과설계임 |

### Must/Should/Could 및 영향 범위

| 하위 항목 | 분류 | 난이도 | 영향 범위 |
|---|---|---|---|
| 케이스 스터디 저장 방식(A/B/C) 확정 | Must (1단계 선행 조건) | 결정 자체는 하, 이후 구현 난이도는 선택지별로 상이 | `docs/PRD.md`(DB 필드 표), `types/project.ts`, `lib/notion.ts`, `shrimp-rules.md` 5절(필드 매핑 표) |
| 카테고리/태그 분리 여부 (4단계 필터링과 연동) | Should | 하 (Single-select 필드 1개 추가 수준) | 동일 |
| 관계형 서브 DB(옵션 C) 채택 | 비권장 | 상 | 과설계 판단, 이번 V2 범위에서 제외 권장 |

### planner 권장안 (최종 결정은 사용자 몫)
- **본문 콘텐츠(케이스 스터디)**: PRD의 근본 설계 전제가 "비개발자도 Notion 자체에서 콘텐츠 관리"이므로, 장문 서사 콘텐츠는 속성 패널보다 페이지 본문에 자연스럽게 녹아드는 **옵션 B**가 철학적으로 더 맞습니다. 다만 구현 난이도가 유의미하게 높으므로(블록 렌더러 신규 구현), 일정이 촉박하면 **옵션 A로 시작 후 옵션 B로 마이그레이션**하는 단계적 접근도 현실적입니다. 최종 선택은 사용자 확인 필요.
- **옵션 C(관계형 서브 DB)**: 채택하지 않는 것을 권장합니다. 프로젝트 1건당 케이스 스터디 섹션이 4개로 고정된 단순 구조에 관계형 DB는 API 호출 비용과 구현 복잡도만 늘립니다.
- **카테고리/태그 분리**: 4단계(필터링 고도화)에서 다시 다룹니다.

### 완료 기준
- [x] 케이스 스터디 콘텐츠 저장 방식(A/B/C)이 사용자 확인을 거쳐 확정됨 — 옵션 A(속성 확장) 채택 (2026-07-29)
- [ ] 카테고리/태그 분리 여부가 확정됨 (2단계에서 별도 결정 예정, 이번 1단계 범위 밖)
- [x] 확정된 방식이 `docs/PRD.md`의 Notion DB 필드 표와 `shrimp-rules.md` 5절에 반영됨

---

## 1단계: 프로젝트 상세 페이지 — 케이스 스터디 구조 (요구사항 1 + 5-A)

> 0단계 결정(케이스 스터디 저장 방식)에 의존합니다. 아래 내용은 기존 V2 검토(라우팅/단건조회/카드클릭/캐싱)를 유지하면서, 이번 요청의 "문제 정의/목표/과정/결과" 케이스 스터디 구조를 통합한 것입니다.

### 코드 기준 재확인 (2026-07-29)
`lib/notion.ts`는 `resolveDataSourceId()` → `notion.dataSources.query()` → `parseProject()` 흐름만 존재하고 단건 조회 함수는 없음. `types/project.ts`의 `Project`에는 본문 필드가 없음. `components/project-card.tsx`는 카드 전체를 `<a href={project.link} target="_blank">`로 감싸되, **`project.link`가 `null`이면 앵커 없이 `cardBody`만 반환**함(59~61행).

### 하위 항목 (Must/Should/Could · 난이도 · 영향 범위)

| 하위 항목 | 분류 | 난이도 | 영향 범위 |
|---|---|---|---|
| 라우팅 (동적 라우트 신설) | Must | 중 | `app/` 신규 파일만 추가, 기존 파일 영향 없음 |
| 단건 조회 함수 (`getProjectById`) | Must | 하 | `lib/notion.ts` 추가만, 기존 `getPublishedProjects()` 무변경 |
| 케이스 스터디 4섹션(문제정의/목표/과정/결과) 렌더링 | **Must** (이번 요청으로 격상 — 기존 로드맵의 "본문 콘텐츠 Could"에서 승격) | 0단계 선택지에 따라 하(A)~상(B) | 0단계 선택에 따라 `types/project.ts`/`lib/notion.ts`/신규 렌더러까지 |
| 카드 클릭 동작 결정 | Must | 선택지별 하~중 (아래 표 참고) | `components/project-card.tsx` |
| 404/비공개 처리 | Must | 하 | 신규 `page.tsx` 내부 로직만 |
| 캐싱 전략(`revalidate`) | Should | 하 | 신규 `page.tsx`에 export 한 줄 추가 |
| 타입 확장 | Should (0단계 결정에 종속) | 하 | `types/project.ts` |
| 프로젝트별 동적 메타데이터/OG (요구사항 5-A) | Should | 메타데이터 자체는 하, 동적 OG 이미지 생성은 중~상 | 신규 `app/projects/[id]/` 내 `generateMetadata()`, (선택) `opengraph-image` 파일 컨벤션 |

### 할 일
- **라우팅**: 프로젝트별 상세 페이지 라우트 신설 (예: `app/projects/[id]/page.tsx`).
  - `context7`(`v16.2.9`) 확인 결과 `params`는 `Promise<{ id: string }>`이며 `await params`로 꺼내는 패턴, `notFound()`, 페이지별 `revalidate` export는 표준으로 유지됨. `shrimp-rules.md` 규칙대로 페이지 props 타입은 직접 정의하지 말고 `PageProps<'/projects/[id]'>` 전역 헬퍼를 사용한다.
  - `app/**/page.tsx`는 컴포넌트 컨벤션 예외 대상이므로 `export default async function`을 유지한다.
- **데이터 조회**: `lib/notion.ts`에 단건 조회 함수 추가 (`getProjectById(id)`). 기존 파싱 로직(`parseProject`, `getThumbnailUrl`, `getPlainText`)을 재사용하고, `notion.pages.retrieve({ page_id: id })` + `isFullPage()` 가드 기반으로 구현. 목록 조회와 달리 `resolveDataSourceId()`가 필요 없어 목록 조회보다 오히려 더 단순함.
- **케이스 스터디 콘텐츠 렌더링**: 0단계에서 확정한 저장 방식(A/B/C)에 따라 문제 정의/목표/과정/결과 4개 섹션을 상세 페이지에 순서대로 배치.
  - 옵션 A라면 4개 Rich Text 속성을 각각 섹션 제목 + 본문으로 렌더링(난이도 하).
  - 옵션 B라면 `notion.blocks.children.list()`로 페이지 본문을 가져와 헤딩 기준으로 섹션을 나누고, 블록 타입별 렌더러(문단/이미지/리스트/헤딩 등)를 구현(난이도 상).
  - 어느 옵션이든 "결과" 섹션에는 가능하면 정량적 임팩트(성과 지표, 사용자 수, 성능 개선 폭 등)를 강조하는 것을 콘텐츠 작성 가이드로 권장(요구사항 5의 "설득력" 목적과 직결, 별도 기능이 아니라 콘텐츠 가이드로 흡수).
- **카드 클릭 동작 결정**: 기존 분석 유지 — (a) 카드 클릭 → 상세 페이지, 외부 링크는 상세 페이지 내 버튼으로 이동(난이도 중, MVP 클릭 동작 재정의) / (b) 카드 클릭은 기존처럼 외부 링크 유지, 상세 페이지는 별도 진입점(난이도 하, 회귀 없음) / (c) 둘 다 지원(난이도 중, 마크업 재구성 필요). `project.link`가 `null`인 카드의 처리도 함께 결정 필요. → **사용자 결정 필요**, 난이도·회귀 위험 종합 시 (b)를 우선 권장.
- **캐싱 전략**: `app/page.tsx`의 `revalidate = 600`을 상세 페이지에도 적용할지 검토. 옵션 B(블록 조회) 채택 시 API 호출이 하나 더 늘어나므로 캐싱 값 재검토 여지 있음.
- **동적 메타데이터/OG (5-A)**: 현재 `app/layout.tsx`는 전역 정적 OG 메타데이터만 갖고 있어(코드 확인됨), 상세 페이지가 SNS/메신저로 공유돼도 어떤 프로젝트인지 구분되지 않음. 상세 페이지에 `generateMetadata()`로 프로젝트별 title/description을 반영하고, 여유가 있으면 `opengraph-image` 파일 컨벤션으로 썸네일 기반 OG 이미지를 생성. 채용 담당자/클라이언트에게 링크를 공유할 때의 설득력에 직접 영향을 주는 항목.
- **404/비공개 처리**: `notFound()`(from `next/navigation`)를 서버 컴포넌트에서 조건부 호출하는 표준 패턴 사용(v16.2.9 기준 유효 확인).

### 왜 이 순서인가
케이스 스터디 구조가 이번 요청의 핵심이며, 상세 페이지는 기존 데이터 레이어와 카드 클릭 동작에 직접 영향을 주므로 0단계(CMS 구조) 다음으로 가장 먼저 확정해야 합니다. 동적 OG 메타데이터(5-A)는 상세 페이지가 존재해야 의미가 생기므로 1단계에 종속시켰습니다.

### 완료 기준
- [x] 상세 페이지 라우트가 Next.js 16 컨벤션(`PageProps<'/경로'>` 사용 규칙 포함)에 맞게 정상 동작함 — `app/projects/[id]/page.tsx`
- [x] `lib/notion.ts`의 단건 조회 함수가 실제 Notion API에서 정상적으로 데이터를 가져옴 — `getProjectById()`(React `cache()` 래핑), 실제 Notion DB에 4개 속성 추가 후 dev 서버로 검증
- [x] 문제 정의/목표/과정/결과 4개 섹션이 0단계에서 확정한 방식(옵션 A)으로 정상 렌더링됨
- [x] 카드 클릭 시 외부 링크 이동과 상세 페이지 이동의 공존 방식이 결정되고 구현됨(카드 클릭 → 상세 페이지, 외부 링크는 상세 페이지 내 버튼), `project.link`가 `null`인 카드의 처리 포함
- [x] 비공개 프로젝트/존재하지 않는 `id` 접근 시 적절히 처리됨 — `getProjectById()`가 null 반환 시 `notFound()`, curl로 200/404 확인
- [x] 캐싱 전략이 상세 페이지에도 일관되게 적용됨 — `revalidate = 600`
- [x] 상세 페이지별 메타데이터(title/description)가 프로젝트 내용을 반영함 — `generateMetadata()`

---

## 2단계: 필터링 고도화 (요구사항 4 — 신규 기능 아님, 기존 개선)

### 코드 기준 재확인 (2026-07-29, 2차 — 0/1단계 완료 후 재검증)
`components/tag-filter.tsx`, `components/project-showcase.tsx`, `types/project.ts`, `lib/notion.ts`를 다시 직접 읽어 확인한 결과, 1단계 작업(케이스 스터디 4개 속성 추가)은 이 2단계 관련 코드에 영향을 주지 않았고 **기존 서술이 그대로 유효합니다.** 다중 태그 선택 + OR 조건 필터링(`ProjectShowcase`의 `selected` 배열 상태, `projects.filter((p) => p.tags.some((tag) => selected.includes(tag)))`)은 여전히 그대로이며, 추가로 확인된 사항:
- `types/project.ts`의 `Project`에는 `tags: string[]` 단일 필드만 있고 카테고리 필드는 없음 — "카테고리/태그 미분리" 한계 재확인.
- `TagFilter`/`ProjectShowcase` 모두 `useState`만 사용하고 `useSearchParams`/`useRouter` import가 없음 — "URL 미동기화" 한계 재확인.
- 태그별 개수 계산 로직, AND/OR 토글 UI 모두 없음 — 재확인.

즉 이번 요구사항은 **신규 필터링 기능 개발이 아니라, 기존 `TagFilter`/`ProjectShowcase`의 잔여 한계를 개선하는 작업**으로 재정의합니다.

확인된 잔여 한계:
1. 카테고리와 태그의 구분이 없음 — PRD의 "태그"(Multi-select) 필드 하나에 기술 스택/카테고리가 뒤섞여 있음.
2. 태그 간 AND 조건(교집합)이 불가능 — 현재는 OR(합집합)만 지원.
3. 필터 상태가 URL에 반영되지 않음 — 새로고침/공유 시 필터 선택이 유지되지 않음(클라이언트 `useState`로만 관리).
4. 태그별 프로젝트 개수 표시가 없음 — 몇 개나 매칭되는지 미리 알 수 없음.
5. 모든 필터링이 클라이언트에서 이미 로드된 전체 목록을 대상으로 처리됨 — 현재 규모(V1 검증 시점 기준 Notion DB 항목 0건)에서는 문제 없으나, 프로젝트 수가 크게 늘어나면 재검토 필요.

### Must/Should/Could 및 영향 범위

| 하위 항목 | 분류 | 난이도 | 영향 범위 |
|---|---|---|---|
| 카테고리/태그 필드 분리 | Should — **사용자 확인 필요** | 중 (Notion 신규 Single-select 속성 1개) | `docs/PRD.md`, `types/project.ts`, `lib/notion.ts`, `components/tag-filter.tsx`, `shrimp-rules.md` 5절 |
| 필터 상태 URL 쿼리 동기화 | Should | 하 | `components/project-showcase.tsx` (Client Component, 이미 `"use client"`) |
| 태그별 프로젝트 개수 뱃지 | Could | 하 | `components/tag-filter.tsx` |
| AND/OR 토글 | Could | 하~중 | `components/project-showcase.tsx` 필터링 로직 |
| 서버 사이드 필터링으로 전환 | 향후 후보(현재 규모에서 불필요) | 중 | `lib/notion.ts`, `app/page.tsx` |

### 할 일 (실행 순서)
1. **카테고리/태그 분리 여부 확정 (사용자 확인 필요, 선행 작업)**: 0단계에서 "2단계에서 별도 결정 예정"으로 보류된 항목. 두 가지 선택지 중 결정 필요.
   - (a) **현행 유지** — Notion "태그" Multi-select 필드 하나로 기술 스택/카테고리를 계속 함께 관리. 신규 필드 없음, 난이도 없음. 현재 프로젝트 수가 적어(V1 검증 시점 0건) 분리 효과가 아직 실증되지 않았다는 점에서 planner는 이 방식을 1차로 권장.
   - (b) **분리** — Notion에 "카테고리" Single-select 속성 신규 추가. `docs/PRD.md` 필드 표, `shrimp-rules.md` 5절 매핑 표, `types/project.ts`(`category: string | null` 등), `lib/notion.ts`의 `parseProject()`를 함께 수정해야 함. `TagFilter`에 카테고리 드롭다운/탭을 별도 UI로 추가.
   - → 결정 결과에 따라 이후 하위 작업의 영향 범위가 달라지므로 반드시 먼저 확정.
2. **필터 상태 URL 쿼리 동기화 구현**: `context7`(`/vercel/next.js/v16.2.9`) 확인 결과 App Router에서는 `next/navigation`의 `useSearchParams()`(읽기, 읽기 전용 `URLSearchParams` 반환)와 `useRouter().push()`/`replace()`(쓰기)를 조합하는 패턴이 표준이며, `useSearchParams`를 사용하는 컴포넌트는 상위에 `<Suspense>` 경계가 필요합니다.
   - `ProjectShowcase`는 이미 `"use client"` 컴포넌트이고, 상위 `app/page.tsx`에서 `ProjectShowcaseSection`을 이미 `<Suspense fallback={<ProjectShowcaseSkeleton />}>`로 감싸고 있으므로(데이터 페칭용) 이 경계를 그대로 재사용 가능한지 우선 검토 — 안 되면 `ProjectShowcase` 내부에 별도 `<Suspense>` 하위 컴포넌트 분리 필요.
   - 쿼리 파라미터 스키마: `?tags=react,typescript` 형식(콤마 구분 단일 파라미터) 권장 — 파라미터 개수가 늘어나도 URL이 지저분해지지 않음.
   - 구현 순서: (1) `ProjectShowcase` 마운트 시 `searchParams.get('tags')?.split(',') ?? []`로 초기 `selected` 상태 계산, (2) 태그 토글 시 `router.replace(pathname + '?' + createQueryString(...))` 호출(브라우저 히스토리 오염 방지를 위해 `push`가 아닌 `replace` 권장), (3) 직접 URL 접근/새로고침 시 필터가 정상 적용되는지, "전체" 클릭 시 쿼리가 제거되는지 확인.
3. **(Could) 태그별 프로젝트 개수 뱃지**: `ProjectShowcase`에서 `projects`를 순회해 `Record<string, number>` 형태의 태그별 카운트를 계산 후 `TagFilter`에 `tagCounts` prop으로 전달, `TagFilter`는 각 `Badge` 옆에 개수를 작게 표시.
4. **(Could) AND/OR 토글**: `ProjectShowcase`에 `mode: 'OR' | 'AND'` 상태 추가, 토글 스위치/버튼 UI 추가, 필터링 로직을 `some`(OR)과 `every`(AND, 단 `selected.every(tag => p.tags.includes(tag))`)로 분기.
5. **회귀 확인**: 기존 MVP 동작(태그 클릭 시 매칭 카드만 노출, "전체" 클릭 시 전체 노출, 빈 상태 안내 문구)이 위 변경 이후에도 동일하게 동작하는지 수동 확인.

### 왜 이 순서인가
0단계/1단계와 데이터 의존성이 없어 병행 진행이 가능합니다. 다만 "카테고리/태그 분리"는 0단계에서 다루는 Notion 스키마 결정과 같은 성격의 작업이므로 그 논의와 묶어 처리하는 것이 효율적입니다. URL 쿼리 동기화는 카테고리/태그 분리 여부와 무관하게 독립적으로 구현 가능하므로 분리 결정을 기다리지 않고 먼저 착수해도 됩니다. 프로젝트 수가 현재 매우 적은 상태(V1 검증 시점 0건)이므로 "탐색성" 문제 자체가 아직 실증되지 않았다는 점을 감안해, 서버 사이드 필터링 전환 등 규모 대응 작업은 향후 후보로 내렸습니다.

### 구현 완료 (2026-07-29)
- 카테고리/태그 분리: **(a) 현행 유지**로 확정 — planner 권장안 채택, 신규 Notion 필드 없음.
- 필터 상태 URL 쿼리 동기화: `components/project-showcase.tsx`에서 `useSearchParams`/`useRouter`/`usePathname`으로 구현. `selected`/`mode`를 로컬 상태 없이 URL(`?tags=react,typescript&mode=and`)에서 직접 파생시켜 새로고침·공유 시에도 항상 URL과 일치하도록 함. `router.replace(..., { scroll: false })`로 히스토리 오염 방지.
- (Could) 태그별 개수 뱃지: `TagFilter`에 `tagCounts`/`totalCount` prop 추가, 각 `Badge`에 개수 표시.
- (Could) AND/OR 토글: `Tabs`(`태그 일부 포함` / `태그 모두 포함`)로 구현, 태그를 2개 이상 선택했을 때만 노출(1개 이하에서는 OR/AND 결과가 동일하므로).
- 회귀 확인: `npx tsc --noEmit`, `next build`(정적 페이지 생성 및 `useSearchParams` Suspense 경계 정상 확인), `eslint`, dev 서버 curl 스모크 테스트(홈 200, 쿼리 파라미터 포함 200, 태그 카운트 뱃지 렌더링 확인)로 검증. 기존 MVP 필터링 동작(태그 클릭 시 매칭 카드만 노출, "전체 보기" 버튼) 유지됨.

### 완료 기준
- [x] 카테고리/태그 분리 여부가 확정되고(사용자 확인), 결정된 경우 필터 UI에 반영됨 — 현행 유지로 확정, UI 변경 없음
- [x] 필터 선택 상태가 URL 쿼리로 유지되어 새로고침/공유 시 보존됨
- [x] (Could 채택 시) 태그별 개수 표시, AND/OR 토글이 정상 동작함 — 둘 다 채택 및 구현됨
- [x] 기존 MVP 필터링 동작(태그 클릭 시 매칭 카드만 노출)이 회귀 없이 유지됨

---

## 3단계: 포트폴리오 목적 고도화 제안 (요구사항 5)

> 사용자 원문(코디네이터 확인): *"추가로 포트폴리오 웹사이트라는 목적을 고려해서 필요한 고도화 기능이 있다면 우선순위와 함께 제안해줘."* 이에 따라 아래 항목들을 정식 제안으로 다룹니다. PRD Out of Scope(검색/페이지네이션/다국어/방문자 분석)는 이번 요청에 명시적으로 포함되지 않았으므로 Must로 올리지 않고 기존처럼 "향후 후보"에 유지합니다.

### 코드 기준 재확인 (2026-07-29, 2차 — 1단계 완료 후 재검증)
- `components/contact-form.tsx`: `grep`으로 프로젝트 전체를 확인한 결과 여전히 **어떤 `app/**` 파일에서도 import되지 않는 미사용 컴포넌트**입니다. 서술 그대로 유효.
- `components/layout/footer.tsx`: `"TODO: 실제 포트폴리오 소유자 이름과 연락 수단(GitHub/LinkedIn 등) 링크로 교체"` 주석과 플레이스홀더 값(`PORTFOLIO_OWNER_NAME = "포트폴리오"`, `CONTACT_URL = "https://github.com"`)이 그대로 남아있음. 서술 그대로 유효.
- `types/project.ts`: `order: number | null` 필드가 존재하고 `lib/notion.ts`의 `getPublishedProjects()`가 이미 `정렬순서` 기준 오름차순으로 서버 정렬해 반환함. 다만 **현재 `ProjectCard`/`ProjectShowcase`에는 "대표/Featured" 표시 UI가 전혀 없음** — 정렬만 될 뿐 시각적으로 강조되지 않음. "기존 필드 재사용으로 구현 가능"이라는 기존 서술이 유효함을 재확인.
- `app/projects/[id]/page.tsx`: 1단계에서 완료 표시된 "동적 메타데이터"는 `generateMetadata()`로 `title`/`description`/`openGraph.title`/`openGraph.description`(텍스트)만 반환하며, **`opengraph-image` 파일 컨벤션이나 `ImageResponse` 기반 동적 OG 이미지 생성은 아직 구현되어 있지 않음** (`find app -iname "*opengraph*"` 결과 없음). 1단계 완료 기준 문구("title/description이 프로젝트 내용을 반영함")는 이 범위에서는 충족되었으나, "동적 OG **이미지**"는 별도의 미완료 항목으로 이번 3단계에서 다룸.

이상 확인 결과 기존 로드맵 서술과 실제 코드 상태 사이에 실질적인 불일치는 없었습니다 (동적 OG 이미지 미구현이라는 세부 사항만 명확화).

포트폴리오 사이트의 목적(채용 담당자/클라이언트에게 프로젝트 임팩트를 설득력 있게 보여주는 것)을 기준으로 제안합니다.

| 제안 항목 | 분류 | 난이도 | 영향 범위 | 비고 |
|---|---|---|---|---|
| **연락처/소개 영역 실제화** (`ContactForm` 연결, `Footer` 플레이스홀더 교체) | **Should — Quick win** | 하 | `components/layout/footer.tsx`, 신규 `app/contact/page.tsx` | `ContactForm`은 이미 구현되어 있으나 미사용, `Footer`에는 TODO가 그대로 남아있음(위 재확인 결과). 신규 기능이 아니라 기존 자산을 실제로 배치/교체하는 작업이라 우선순위 대비 효과가 큼 |
| **대표(Featured) 프로젝트 강조** | Should | 하(기존 `order` 필드 재사용) | `components/project-card.tsx` 또는 `components/project-showcase.tsx` | 채용 담당자가 전체를 다 보지 않고 임팩트 큰 프로젝트를 먼저 보게 하는 장치. `order` 필드가 이미 서버 정렬에 쓰이고 있으므로 신규 Notion 필드 없이 "상위 N개=대표" 배지만 UI에 추가하면 됨 |
| **프로젝트별 동적 OG 이미지** | Should | 중~상 | 신규 `app/projects/[id]/opengraph-image.tsx` | 1단계에서 텍스트 메타데이터(title/description)는 이미 구현됨(5-A 완료). 이번 3단계에서는 그 다음 단계인 **이미지** 생성만 다룸(아래 할 일 참고) |
| **케이스 스터디 "결과" 섹션에 정량적 임팩트 지표 가이드** | Could (별도 기능이 아닌 콘텐츠 작성 가이드) | 하 | 1단계 콘텐츠 구조, 문서 가이드 | 신규 Notion 필드보다는 "결과" 속성 안에 성과 지표를 적극적으로 담도록 작성 가이드로 흡수 |
| **프로젝트 기간/역할/팀 구성 메타정보** | Could — 사용자 확인 필요 | 중 | `docs/PRD.md` 스키마, `types/project.ts`, `lib/notion.ts`, 상세 페이지 | 채용 담당자가 자주 궁금해하는 정보지만 현재 Notion DB 스키마에 없음. 신규 Notion 속성 추가가 필요하므로 착수 전 사용자 확인 필요 |
| **접근성/성능 재점검** | Should | 하(점검 자체) | 배포 전 QA 체크리스트 | V1 5단계에서 Lighthouse 93/100/100/100을 확인했으나 "실제 썸네일이 없어 이미지 로딩이 반영되지 않은 수치"라는 기록이 남아있음. 1~3단계에서 상세 페이지/필터/연락처 페이지 등 신규 화면이 추가됐으므로 재점검 필요 |

### 할 일 (실행 순서)
1. **연락처 페이지 신설 및 Footer 연결 (Quick win, 최우선)**:
   - `shrimp-rules.md` 4절 규칙대로 신규 `app/contact/page.tsx`를 만들고 `Container`로 감싼 뒤 `ContactForm`을 배치(`export default async function` 유지 불필요 — 인터랙션 없는 정적 서버 컴포넌트로 작성, `ContactForm` 자체가 `"use client"`이므로 페이지는 서버 컴포넌트로 유지 가능).
   - `components/layout/footer.tsx`의 TODO 플레이스홀더(`PORTFOLIO_OWNER_NAME`, `CONTACT_URL`)를 실제 값으로 교체 — **실제 이름/연락 수단(GitHub·LinkedIn·이메일 등)은 사용자 확인 필요**, 없으면 자리표시자만 정리하고 실제 값 입력은 별도 작업으로 남김.
   - `Footer`에 `/contact` 링크 추가 검토(현재는 외부 GitHub 링크만 있음).
2. **대표(Featured) 프로젝트 강조**:
   - 표시 방식 결정 필요(사용자 확인 권장): (a) 상위 N개 카드에 "Featured" `Badge`만 추가, (b) 그리드 상단에 별도 "대표 프로젝트" 섹션으로 분리. 난이도·회귀 위험이 낮은 (a)를 1차로 권장.
   - `order` 값이 없는(`null`) 프로젝트가 섞여 있을 때의 처리(예: 정렬 마지막으로 밀림, Featured 대상에서 제외)를 함께 정의.
3. **프로젝트별 동적 OG 이미지**:
   - `context7`(`/vercel/next.js/v16.2.9`) 확인 결과, `app/projects/[id]/opengraph-image.tsx` 파일 컨벤션으로 `ImageResponse`(`next/og`)를 사용해 동적 이미지를 생성하며, `params`는 `Promise<{ id: string }>`로 받아 `await` 필요(1단계와 동일 패턴).
   - `getProjectById()`를 재사용해 프로젝트 제목/썸네일을 이미지에 반영. 프로젝트 썸네일이 없는 경우(placeholder) 처리 방식도 함께 정의.
   - 별도 `alt`, `size = { width: 1200, height: 630 }`, `contentType = 'image/png'` export 필요.
4. **케이스 스터디 결과 섹션 작성 가이드**: 코드 변경 없이 `docs/PRD.md` 또는 신규 콘텐츠 가이드 섹션에 "결과 속성에는 정량 지표(사용자 수, 성능 개선 폭, 완료 기간 단축 등)를 우선 기입" 문구 추가.
5. **프로젝트 기간/역할/팀 구성 메타정보**: 착수 전 사용자에게 (a) 진행 여부, (b) 진행 시 신규 Notion 속성 이름/타입 확인 후 0단계와 동일한 절차(PRD/타입/파서/`shrimp-rules.md` 동시 갱신)로 진행.
6. **Lighthouse 접근성/성능 재점검**: 위 1~4 항목(및 2단계 필터 변경) 배포 후 실제 콘텐츠(썸네일 포함)가 채워진 상태로 재측정.

### 왜 이 순서인가
"연락처/소개 영역 실제화"는 이미 만들어져 있는 컴포넌트를 배치만 하면 되는 가장 낮은 난이도·즉시 가치 항목이라 가장 먼저 착수 가능한 Quick win으로 분류했습니다. 동적 OG 이미지는 1단계에서 이미 구현된 텍스트 메타데이터의 자연스러운 후속 작업이므로 그다음으로 배치했습니다. 신규 Notion 필드가 필요한 항목(기간/역할/팀 구성)은 사용자 확인이 우선이므로 뒤로 미뤘고, Lighthouse 재점검은 이 단계의 모든 신규 화면이 반영된 뒤 의미가 있으므로 마지막에 둡니다.

### 완료 기준
- [ ] `ContactForm`이 실제 페이지(`/contact`)에 연결되고 `Footer`의 플레이스홀더가 실제 정보로 교체됨
- [ ] 대표 프로젝트 강조 방식이 결정되고(`order` 필드 기반) 그리드에 반영됨
- [ ] 프로젝트별 동적 OG **이미지**(`opengraph-image` 파일 컨벤션)가 정상 동작함 (텍스트 메타데이터는 1단계에서 이미 완료)
- [ ] 케이스 스터디 "결과" 섹션 작성 가이드가 문서에 반영됨
- [ ] (진행 확정 시) 프로젝트 기간/역할/팀 구성 메타정보가 스키마·타입·파서·문서에 일관되게 반영됨
- [ ] 신규 컴포넌트 추가 이후 Lighthouse 재점검이 수행됨

---

## 4단계: 관리자 Dashboard — 미진행 (해당 없음, V2 범위 제외) (요구사항 2 — 비판적 검토, 범위 축소 권장)

> **사용자 결정 완료 (2026-07-29)**: 갈래 B(미진행) 확정. 이번 V2에서는 관리자 대시보드를 만들지 않으며, 향후 후보로 이동합니다. 아래 검토 내용(PRD와의 긴장 관계, 옵션별 분석)은 왜 미진행을 택했는지의 근거 기록으로 그대로 남겨둡니다.

### PRD와의 긴장 관계 (반드시 먼저 짚어야 할 지점)
`docs/PRD.md`의 CMS 선택 이유는 명시적으로 **"Notion API를 활용하여 비개발자도 콘텐츠 관리 가능 (재배포 없이 Notion에서 프로젝트 추가·수정·태그 변경 가능)"**입니다. 즉 "콘텐츠 관리 UI"는 이미 Notion 자체로 해결하도록 설계되어 있습니다. 별도 관리자 대시보드를 만든다면, 그것이 Notion과 다른 어떤 가치를 제공하는지 반드시 구분해야 합니다.

- **쓰기 가능한 CRUD 관리 도구** (프로젝트 추가/수정/발행 상태 변경 등을 대시보드에서 직접 수행): PRD의 "Notion = CMS" 전제와 정면으로 중복됩니다. Notion에 이미 있는 기능을 재구현하는 것이며, 추가로 다음 문제가 발생합니다.
  - **인증/인가 필요**: `context7`(`/vercel/next.js/v16.2.9`) 확인 결과, Next.js 16에서는 미들웨어 파일명이 `middleware.ts` → **`proxy.ts`**로 변경되었습니다. 세션 쿠키 기반 인증(`decrypt(cookie)` 후 `NextResponse.redirect`), 또는 역할 기반 접근 제어(`next/navigation`의 `forbidden()`) 패턴이 공식 문서에 존재하지만, 이 프로젝트에는 현재 인증 라이브러리/세션 저장소가 전혀 없습니다(코드 확인: `package.json`에 auth 관련 의존성 없음, `proxy.ts`/`middleware.ts` 파일 없음).
  - **동기화 문제**: 대시보드에서 쓴 값과 Notion 원본 사이의 동기화(둘 중 어느 쪽이 "진실의 원천"인지), 동시 편집 충돌 등 새로운 복잡도가 생깁니다.
  - **보안 위험**: 쓰기 API를 외부에 노출하면 공개 배포된 개인 포트폴리오 사이트치고는 과도한 공격 표면이 생깁니다.
  - → **작업 원칙("새로운 기능보다 기존 구조 개선 우선") 및 이번 검토 결과, 쓰기 가능 CRUD 관리 도구는 이 프로젝트 규모/목적에 비해 과잉설계로 판단해 로드맵에서 제외를 권장합니다.**

- **읽기 전용 요약 대시보드** (예: 전체 프로젝트 발행 상태 한눈에 보기, 썸네일/링크 누락 경고, 마지막 수정 시각, 태그별 분포 통계 등): Notion 자체의 테이블/보드 뷰로도 상당 부분 커버되지만, "이 사이트에 지금 무엇이 노출되고 있는가"를 웹에서 바로 확인하고 싶은 경우엔 나름의 가치가 있습니다. 다만 이 경우에도 **비공개(`공개여부=false`) 프로젝트 데이터를 화면에 노출**하게 되므로(`shrimp-rules.md` 8절: "클라이언트 컴포넌트에서 비공개 프로젝트 데이터를 필터링하는 것 금지" — 즉 비공개 데이터 자체가 서버 밖으로 나가면 안 된다는 원칙과 맞닿아 있음), **최소한의 접근 제어**(예: 단일 소유자용 공유 비밀 토큰을 `proxy.ts`에서 검사하는 수준)는 필요합니다. 다인증/회원 시스템까지는 필요 없습니다.

### 코드 기준 재확인 (2026-07-29, 2차)
`proxy.ts`, `middleware.ts` 파일이 여전히 저장소 루트에 없고(`ls` 결과 없음), `package.json`의 `dependencies`/`devDependencies`에 인증/세션 관련 패키지(`next-auth`, `iron-session`, `jose`, `lucia` 등)가 여전히 전혀 없음을 재확인했습니다. 즉 4단계의 전제("이 프로젝트에는 인증 인프라가 없다")는 여전히 유효합니다.

### ✅ 진행 여부 확정 — 갈래 B (미진행)
사용자가 "관리자 대시보드를 이번 V2에서 만들지 않는다"를 확정했습니다(2026-07-29). 아래는 확정에 따른 후속 처리입니다.

1. 4단계는 "해당 없음(Not Applicable)"으로 이 문서에 명시하고, V2 완료 기준에서 제외합니다.
2. "관리자 대시보드"는 향후 후보 목록으로 이동시킵니다(아래 "향후 후보" 절 참고). 향후 명시적 요청이 있을 때 별도 버전에서, 인증 체계 자체를 먼저 설계하는 단계부터 재검토합니다.
3. V2는 0/1/2/3단계 완료만으로 아카이빙 가능합니다.

참고로 만약 향후 버전에서 재검토하게 될 경우를 대비해, 검토 시점에 다시 확인해야 할 두 갈래(쓰기 가능 CRUD는 계속 비권장 / 읽기 전용 요약 대시보드 + 경량 토큰 접근 제어)의 분석은 위 "PRD와의 긴장 관계" 절에 그대로 남겨둡니다.

### Must/Should/Could 및 영향 범위

| 하위 항목 | 분류 | 난이도 | 영향 범위 |
|---|---|---|---|
| 쓰기 가능 CRUD 관리 도구 | **비권장 (로드맵 제외)** | 상 (인증/보안/동기화 포함) | 해당 없음 — V2에서 진행하지 않음 |
| 읽기 전용 발행 현황 요약 대시보드 | 미진행 (향후 후보로 이동) | 중 (경량 접근 제어 포함) | 해당 없음 — V2에서 진행하지 않음 |

### 왜 미진행인가
PRD의 근본 설계("Notion = CMS, 비개발자도 Notion에서 직접 관리")와 가장 크게 충돌할 소지가 있고, 인증/보안이라는 이 프로젝트에 없는 새로운 축을 요구하는 데 비해 현재 프로젝트 규모(콘텐츠 건수, 협업 인원)에서 얻는 가치가 크지 않다고 판단해 미진행으로 확정했습니다.

### 완료 기준
- [x] 4단계 진행 여부가 사용자 확인을 거쳐 확정됨 — 갈래 B(미진행)
- [x] 이 단계가 "해당 없음"으로 문서에 명시되고 V2 완료 기준에서 제외됨

---

## 향후 후보 (V2 범위 밖, 우선순위 낮음)

- 검색 기능
- 페이지네이션/무한스크롤
- 다국어 지원
- 방문자 분석
- 서버 사이드 필터링 전환 (2단계 참고 — 프로젝트 수가 실제로 늘어난 뒤 재평가)
- 관리자 대시보드 전체(읽기 전용 요약 포함, 쓰기 가능 CRUD도 포함) — V2에서 미진행 확정(2026-07-29, 갈래 B). 향후 명시적 요청이 있다면 별도 버전에서 인증 체계부터 재설계
- 프로젝트 기간/역할/팀 구성 메타정보 (3단계 참고 — 0단계 CMS 구조 결정 시 함께 재검토)

---

## V2 전체 완료 기준

- [x] 0단계(CMS 구조 확장 방향)의 완료 기준을 모두 충족함
- [x] 1단계(프로젝트 상세 페이지 — 케이스 스터디 구조)의 완료 기준을 모두 충족함
- [x] 2단계(필터링 고도화)의 완료 기준을 모두 충족함
- [ ] 3단계(포트폴리오 목적 고도화 제안)의 완료 기준을 모두 충족함
- [x] 4단계(관리자 대시보드): **미진행 확정(갈래 B)** — "해당 없음"으로 처리, V2 완료 판정에서 제외
- [ ] 기존 MVP 기능(카드 그리드, 태그 필터링, 외부 링크)이 위 변경 이후에도 정상 동작함 (회귀 없음)

### 아카이빙 절차 (V2 완료 시 수행)
위 완료 기준(2, 3단계 — 4단계는 미진행 확정으로 판정 대상에서 제외)이 모두 충족되면 다음 순서로 진행합니다. **이 절차는 실제 구현이 모두 끝난 뒤 수행하는 후속 작업이며, 이번 계획 수립 시점에는 아직 착수하지 않습니다.**

1. 사용자에게 "V2 핵심 단계가 모두 완료되었으니 아카이빙을 진행해도 되는지" 먼저 확인합니다(버전 관리 규칙에 따라 과거 버전 파일은 함부로 만들거나 덮어쓰지 않음).
2. 확인 후 이 시점의 `docs/roadmap.md` 전체 내용을 `docs/roadmaps/roadmap_v2.md`(소문자, 신규 파일)로 복사합니다.
3. `docs/roadmap.md`를 V3 계획으로 새로 작성합니다. V3 후보는 "향후 후보" 절(검색, 페이지네이션/무한스크롤, 다국어, 방문자 분석, 서버 사이드 필터링, 관리자 대시보드 전체, 기간/역할/팀 메타정보 등)과 V2 진행 중 새로 식별된 개선 후보를 함께 검토해 구성합니다.
4. `CLAUDE.md`의 `@docs/roadmap.md` 참조는 파일 경로 자체가 바뀌지 않으므로 별도 수정이 필요 없는지 재확인합니다(참조 대상은 항상 "현재 버전"이므로 내용만 교체되면 됩니다).
5. `docs/roadmaps/ROADMAP_v1.md`는 이 과정에서 수정하지 않습니다.
